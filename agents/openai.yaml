interface:
  display_name: "Codex Auto Model Router"
  short_description: "为 GPT-5.6 轻量选模、分配推理强度与有用并发"
  default_prompt: "Use $codex-auto-model-router in Router Lite mode: select the lowest sufficient GPT-5.6 route, keep short work local, safely reuse a compatible same-request executor once, parallelize only when activation-aware net benefit is positive, fail open locally, and never auto-select Ultra."
