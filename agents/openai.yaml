interface:
  display_name: "Codex Auto Model Router"
  short_description: "为 GPT-5.6 动态选择模型、low 至 max 推理与安全并发"
  default_prompt: "Use $codex-auto-model-router to re-evaluate this request, use Luna medium for mechanical work, Luna high for ordinary bounded work and normal scans, Luna xhigh for large bounded scans, and Luna max only for large deterministic deep work; reserve Terra high for explicit latency priority and Sol for real complexity or consequence. For parallel work persist the plan once, issue hash-bound bounded tickets, attach executors by ID, never auto-select Ultra, stop on failure, and report the runtime-generated current-run concurrency brief."
