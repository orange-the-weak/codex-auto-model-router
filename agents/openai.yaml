interface:
  display_name: "Codex Auto Model Router"
  short_description: "为 GPT-5.6 自动选择模型、推理与安全并发上限"
  default_prompt: "Use $codex-auto-model-router to re-evaluate this Apply request, choose GPT-5.6 routes for the smallest useful bounded task graph, run genuinely independent work with critical-path-priority wait-any scheduling, cap automatic concurrency at 4 and smart-reduce it to confirmed useful capacity, create workers only for free slots, preserve safe ownership, stop on failure, record only verified usage and timing, and end the brief with concise concurrency and correctly labeled speedup status."
