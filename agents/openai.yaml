interface:
  display_name: "Codex Auto Model Router"
  short_description: "自动选择 GPT-5.6 路由，支持项目级退出与恢复"
  default_prompt: "Use $codex-auto-model-router to choose the lowest sufficient GPT-5.6 route, honor project-level exit settings, prefer safe direct tool concurrency, and explain why execution stays on the current coordinator when no model-specific leaf is used."
